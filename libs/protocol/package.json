{
  "name": "@nyrvexa/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Wire types shared between Nyrvexa web app and gateway.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
